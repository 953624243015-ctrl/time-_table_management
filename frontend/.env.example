# Copy to .env.local for local dev (optional - proxy handles it automatically)
# For production (Vercel), set this as Environment Variable in Vercel dashboard
VITE_API_URL=https://your-render-backend.onrender.com
